# 3. Config

12 Factor app stores config environment variable to avoid the failure while running same backing service instance in multiple environments such as Prod, Staging & Dev. These Variables stored in .env files on the Prod, Staging & Dev environments with the details of the configuration details, respectively.
