# 7. Port Binding

To receive & send the request to an app we need a port binding. In this case the redis app is binded with a port 6379 by default. E.g. flask framework is also binded with a port 5000 by default, etc.

Unlike traditional web applications, 12 factor is completely self-contained & does not rely on a specific web server to function.
